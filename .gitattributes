# Normalise line endings in the index; check out whatever the platform uses.
* text=auto

# Shell scripts run on Linux runners, so they keep LF on every checkout.
*.sh text eol=lf

# Never normalised, never diffed: the strong-name key and the package icon.
*.snk binary
*.png binary
